export enum gameStatuses {
  idle = "idle",
  playing = "playing",
  won = "won",
  lost = "lost",
}

export enum gameModes {
  free = "free",
  bullet = "bullet",
  classic = "classic",
  none = "none",
}
